---
phase: 02-home-battery-schedule
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/energy_manager/const.py
  - custom_components/energy_manager/coordinator.py
  - custom_components/energy_manager/sensor.py
  - custom_components/energy_manager/number.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Schedule slot prices display with at most 4 decimal places (no IEEE 754 artifacts)"
    - "Next Charging Slot sensor shows available (green) with no state when no slots scheduled, not Unknown/red"
    - "Next Discharging Slot sensor shows available (green) with no state when no slots scheduled, not Unknown/red"
    - "Charge Price Threshold defaults to 1.0 SEK/kWh for new installations"
    - "Discharge Price Threshold defaults to 0.50 SEK/kWh for new installations"
    - "Max Charge Power displays in kW with 0.1 step, 15.0 max, 5.0 default"
    - "Scheduler still receives max_charge_power in watts (kW * 1000 conversion)"
  artifacts:
    - path: "custom_components/energy_manager/const.py"
      provides: "Updated defaults and kW-scale constants"
      contains: "DEFAULT_CHARGE_THRESHOLD = 1.0"
    - path: "custom_components/energy_manager/coordinator.py"
      provides: "Rounded price in _serialize_slot, kW-to-W conversion"
      contains: "round(slot.price, 4)"
    - path: "custom_components/energy_manager/sensor.py"
      provides: "available() property on NextCharge/NextDischargeSensor, rounded price in schedule attributes"
      contains: "def available"
    - path: "custom_components/energy_manager/number.py"
      provides: "kW unit, 0.1 step, kW-to-W coordinator update"
      contains: "kW"
  key_links:
    - from: "custom_components/energy_manager/number.py"
      to: "coordinator.max_charge_power_w"
      via: "kW * 1000 conversion in async_set_native_value and async_added_to_hass"
      pattern: "max_charge_power_w.*\\*.*1000"
    - from: "custom_components/energy_manager/const.py"
      to: "custom_components/energy_manager/number.py"
      via: "Renamed kW constants imported by number.py"
      pattern: "DEFAULT_MAX_CHARGE_POWER_KW"
---

<objective>
Close 5 UAT gaps from Phase 2 user testing: float precision in schedule prices, sensor availability semantics, updated threshold defaults, and max charge power unit change from W to kW.

Purpose: All gaps are minor/cosmetic fixes requested by the user during UAT. Closing them before moving to Phase 3 ensures a clean foundation.
Output: Four modified source files with all UAT issues resolved.
</objective>

<execution_context>
@/Users/johan.yourstone/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johan.yourstone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-battery-schedule/02-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix defaults, float precision, and sensor availability</name>
  <files>
    custom_components/energy_manager/const.py
    custom_components/energy_manager/coordinator.py
    custom_components/energy_manager/sensor.py
  </files>
  <action>
    **const.py** -- Update two default values:
    - Change `DEFAULT_CHARGE_THRESHOLD = 0.50` to `DEFAULT_CHARGE_THRESHOLD = 1.0` (line 61)
    - Change `DEFAULT_DISCHARGE_THRESHOLD = 1.50` to `DEFAULT_DISCHARGE_THRESHOLD = 0.50` (line 62)
    - Leave all other constants unchanged for now (charge power constants handled in Task 2)

    **coordinator.py** -- Fix float precision in `_serialize_slot()`:
    - In the `_serialize_slot` function (line ~460), change `"price": slot.price` to `"price": round(slot.price, 4)`
    - This affects the next_charging_slot and next_discharging_slot dicts in BatteryScheduleData

    **sensor.py** -- Fix float precision in BatteryScheduleSensor AND add availability to NextCharge/NextDischargeSensor:

    1. In `BatteryScheduleSensor.extra_state_attributes` (line ~160), change `"price": slot.price` to `"price": round(slot.price, 4)` in the schedule serialization loop. This is a SEPARATE serialization path from `_serialize_slot` -- both need the fix.

    2. Add an `available` property to `NextChargeSensor`:
    ```python
    @property
    def available(self) -> bool:
        """Return True when coordinator has data (distinguishes no-slots from error)."""
        return self.coordinator.data is not None
    ```
    Place it before the `native_value` property.

    3. Add the same `available` property to `NextDischargeSensor`:
    ```python
    @property
    def available(self) -> bool:
        """Return True when coordinator has data (distinguishes no-slots from error)."""
        return self.coordinator.data is not None
    ```
    Place it before the `native_value` property.

    The effect: When coordinator has data but no slots exist, `available=True` + `native_value=None` renders as "Unknown" with a green/available entity icon (not red/unavailable). When coordinator has NO data (error state), `available=False` renders as "Unavailable" with red icon. This distinguishes "nothing scheduled" from "something is broken".
  </action>
  <verify>
    Run: `python -c "from custom_components.energy_manager.const import DEFAULT_CHARGE_THRESHOLD, DEFAULT_DISCHARGE_THRESHOLD; assert DEFAULT_CHARGE_THRESHOLD == 1.0; assert DEFAULT_DISCHARGE_THRESHOLD == 0.50; print('Defaults OK')"` from the repo root.

    Verify `_serialize_slot` has `round(slot.price, 4)` by grepping: `grep -n "round(slot.price" custom_components/energy_manager/coordinator.py custom_components/energy_manager/sensor.py` -- should show matches in both files.

    Verify available property exists: `grep -n "def available" custom_components/energy_manager/sensor.py` -- should show two matches (NextChargeSensor and NextDischargeSensor).
  </verify>
  <done>
    - DEFAULT_CHARGE_THRESHOLD is 1.0
    - DEFAULT_DISCHARGE_THRESHOLD is 0.50
    - Both _serialize_slot (coordinator.py) and BatteryScheduleSensor.extra_state_attributes (sensor.py) round prices to 4 decimal places
    - NextChargeSensor and NextDischargeSensor both have available() property returning self.coordinator.data is not None
  </done>
</task>

<task type="auto">
  <name>Task 2: Change max charge power from W to kW</name>
  <files>
    custom_components/energy_manager/const.py
    custom_components/energy_manager/number.py
    custom_components/energy_manager/coordinator.py
  </files>
  <action>
    **const.py** -- Rename and rescale charge power constants:
    - Change `DEFAULT_MAX_CHARGE_POWER_W = 5000.0` to `DEFAULT_MAX_CHARGE_POWER_KW = 5.0`
    - Change `MIN_CHARGE_POWER_W = 0.0` to `MIN_CHARGE_POWER_KW = 0.0`
    - Change `MAX_CHARGE_POWER_W = 15000.0` to `MAX_CHARGE_POWER_KW = 15.0`
    - Change `CHARGE_POWER_STEP = 100.0` to `CHARGE_POWER_STEP_KW = 0.1`

    **number.py** -- Update BatteryMaxChargePower entity:
    - Update imports: replace `CHARGE_POWER_STEP, DEFAULT_MAX_CHARGE_POWER_W, MAX_CHARGE_POWER_W, MIN_CHARGE_POWER_W` with `CHARGE_POWER_STEP_KW, DEFAULT_MAX_CHARGE_POWER_KW, MAX_CHARGE_POWER_KW, MIN_CHARGE_POWER_KW`
    - Change `_attr_native_unit_of_measurement = "W"` to `_attr_native_unit_of_measurement = "kW"`
    - Change `_attr_native_min_value = MIN_CHARGE_POWER_W` to `_attr_native_min_value = MIN_CHARGE_POWER_KW`
    - Change `_attr_native_max_value = MAX_CHARGE_POWER_W` to `_attr_native_max_value = MAX_CHARGE_POWER_KW`
    - Change `_attr_native_step = CHARGE_POWER_STEP` to `_attr_native_step = CHARGE_POWER_STEP_KW`
    - Change `_default_value = DEFAULT_MAX_CHARGE_POWER_W` to `_default_value = DEFAULT_MAX_CHARGE_POWER_KW`
    - In `async_added_to_hass`: change `self.coordinator.max_charge_power_w = self._attr_native_value` to `self.coordinator.max_charge_power_w = self._attr_native_value * 1000` (convert kW to W for the scheduler)
    - In `async_set_native_value`: change `self.coordinator.max_charge_power_w = value` to `self.coordinator.max_charge_power_w = value * 1000` (convert kW to W for the scheduler)
    - Update the docstring for `async_set_native_value` to say "in kW" instead of "in watts"

    **coordinator.py** -- Update the default import:
    - Change import `DEFAULT_MAX_CHARGE_POWER_W` to `DEFAULT_MAX_CHARGE_POWER_KW`
    - In `BatteryScheduleCoordinator.__init__`, change `self.max_charge_power_w: float = DEFAULT_MAX_CHARGE_POWER_W` to `self.max_charge_power_w: float = DEFAULT_MAX_CHARGE_POWER_KW * 1000` (coordinator stores watts internally for the scheduler)

    IMPORTANT: The `max_charge_power_w` attribute name on the coordinator stays as `_w` because the scheduler (build_battery_schedule) expects watts. The conversion happens at two boundaries: (1) number entity -> coordinator, and (2) coordinator default initialization. The scheduler call in `_async_update_data` passes `self.max_charge_power_w` unchanged -- no changes needed there.
  </action>
  <verify>
    Run: `python -c "from custom_components.energy_manager.const import DEFAULT_MAX_CHARGE_POWER_KW, MAX_CHARGE_POWER_KW, CHARGE_POWER_STEP_KW; assert DEFAULT_MAX_CHARGE_POWER_KW == 5.0; assert MAX_CHARGE_POWER_KW == 15.0; assert CHARGE_POWER_STEP_KW == 0.1; print('kW constants OK')"` from the repo root.

    Verify kW unit in number.py: `grep -n "kW" custom_components/energy_manager/number.py` -- should show "kW" as unit.

    Verify W conversion: `grep -n "* 1000" custom_components/energy_manager/number.py` -- should show two matches (async_added_to_hass and async_set_native_value).

    Run existing tests: `python -m pytest tests/ -x -q` to confirm nothing is broken.
  </verify>
  <done>
    - Max Charge Power entity displays in kW with step 0.1, max 15.0, default 5.0
    - Coordinator receives watts (kW * 1000) so scheduler behavior is unchanged
    - All old W-suffixed constants replaced with KW-suffixed constants
    - Existing tests pass
  </done>
</task>

</tasks>

<verification>
All 5 UAT gaps addressed:

1. Float precision: `round(slot.price, 4)` in both `_serialize_slot()` (coordinator.py) and `BatteryScheduleSensor.extra_state_attributes` (sensor.py)
2. Unknown vs None: `available()` property on NextChargeSensor and NextDischargeSensor
3. Charge threshold default: 0.50 -> 1.0 in const.py
4. Discharge threshold default: 1.50 -> 0.50 in const.py
5. Max charge power unit: W -> kW with conversion at entity boundary

Verify no import errors: `python -c "from custom_components.energy_manager import const, coordinator, sensor, number; print('All imports OK')"`
</verification>

<success_criteria>
- All 5 UAT gaps resolved
- Existing tests pass (`python -m pytest tests/ -x -q`)
- No import errors across all modified modules
- Coordinator still passes watts to the scheduler (kW * 1000 conversion at entity boundary)
</success_criteria>

<output>
After completion, create `.planning/phases/02-home-battery-schedule/02-04-SUMMARY.md`
</output>
