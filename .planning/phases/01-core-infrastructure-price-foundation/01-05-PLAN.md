---
phase: 01-core-infrastructure-price-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/energy_manager/sensor.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Integration setup completes with no warnings in the HA log about attribute size or state class"
    - "Price sensor still shows current electricity price as state"
    - "Downstream modules can still access full price slot data via coordinator"
  artifacts:
    - path: "custom_components/energy_manager/sensor.py"
      provides: "Price sensor entity with correct state_class and minimal attributes"
      contains: "_attr_device_class = SensorDeviceClass.MONETARY"
      must_not_contain: "SensorStateClass"
  key_links:
    - from: "custom_components/energy_manager/sensor.py"
      to: "coordinator.py"
      via: "PriceCoordinator as data source for native_value"
      pattern: "coordinator\\.data\\.current_price"
---

<objective>
Fix two UAT-reported warnings in the price sensor entity: (1) remove hourly price slot lists from extra_state_attributes to stay under HA's 16KB recorder limit, and (2) delete the incorrect SensorStateClass.MEASUREMENT declaration which is incompatible with SensorDeviceClass.MONETARY.

Purpose: Close the single remaining UAT gap from Phase 01 -- "Integration setup completes with no errors or warnings in the HA log"
Output: Clean sensor.py that passes HA validation without warnings
</objective>

<execution_context>
@/Users/johan.yourstone/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johan.yourstone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-infrastructure-price-foundation/01-04-SUMMARY.md
@.planning/phases/01-core-infrastructure-price-foundation/01-UAT.md
@.planning/debug/price-sensor-warnings.md
@custom_components/energy_manager/sensor.py
@custom_components/energy_manager/coordinator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix price sensor state class and remove oversized attributes</name>
  <files>custom_components/energy_manager/sensor.py</files>
  <action>
Two targeted changes to custom_components/energy_manager/sensor.py:

**Fix 1: Remove wrong state class (line 52)**
- Delete the line `_attr_state_class = SensorStateClass.MEASUREMENT`
- This makes state_class default to None, which is correct for monetary spot prices per HA validation rules
- Remove `SensorStateClass` from the import on line 14 (it will be unused)

**Fix 2: Remove oversized extra_state_attributes (lines 78-107)**
- Replace the current extra_state_attributes property that serializes 48 hourly price slots
- The new extra_state_attributes should return ONLY lightweight metadata:
  - "last_updated": ISO timestamp string (or None)
- Do NOT include "today" or "tomorrow" price slot lists -- downstream modules (battery scheduler, EV scheduler) access full price data directly from the coordinator via `entry.runtime_data.price_coordinator.data`
- Keep the property method and the data-None guard, just strip out the slot serialization

**Why remove attributes entirely instead of trimming:**
- The coordinator is already the canonical data source for all internal modules
- HA best practice: entity attributes are for UI display metadata, not bulk data transfer
- No automations or templates depend on these attributes yet (Phase 1 just shipped)
- Eliminates the 16KB concern permanently rather than dancing around the limit

**What the file should look like after changes:**
- Imports: Remove SensorStateClass from the import block
- Class attrs: _attr_translation_key, _attr_device_class (MONETARY), _attr_native_unit_of_measurement, _attr_suggested_display_precision -- NO _attr_state_class
- native_value property: Unchanged (returns coordinator.data.current_price)
- extra_state_attributes property: Returns only {"last_updated": ...}
  </action>
  <verify>
1. `python -c "import ast; ast.parse(open('custom_components/energy_manager/sensor.py').read()); print('OK')"` -- file parses without syntax errors
2. `grep -c "SensorStateClass" custom_components/energy_manager/sensor.py` returns 0 -- state class fully removed
3. `grep -c "today" custom_components/energy_manager/sensor.py` returns 0 -- no price slot lists in attributes
4. `grep "native_value" custom_components/energy_manager/sensor.py` still shows the property exists -- current price still exposed
5. `grep "last_updated" custom_components/energy_manager/sensor.py` confirms metadata attribute retained
  </verify>
  <done>sensor.py has no SensorStateClass reference, extra_state_attributes returns only last_updated metadata (no today/tomorrow slot lists), native_value still returns current_price from coordinator, and file parses cleanly</done>
</task>

</tasks>

<verification>
- sensor.py parses without syntax errors
- No references to SensorStateClass anywhere in the file
- extra_state_attributes returns a dict with only "last_updated" key (no "today", no "tomorrow")
- native_value property unchanged -- still returns coordinator.data.current_price
- The import block imports only SensorDeviceClass and SensorEntity from homeassistant.components.sensor
- PriceData import still present (used by native_value)
</verification>

<success_criteria>
Both UAT warnings resolved:
1. No "state class 'measurement' impossible" warning -- state_class is now None (default)
2. No "attributes exceed maximum size" warning -- only last_updated metadata in attributes
Price sensor still functions: shows current price as state via coordinator
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure-price-foundation/01-05-SUMMARY.md`
</output>
