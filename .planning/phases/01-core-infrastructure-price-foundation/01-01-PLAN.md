---
phase: 01-core-infrastructure-price-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/energy_manager/manifest.json
  - custom_components/energy_manager/hacs.json
  - custom_components/energy_manager/const.py
  - custom_components/energy_manager/entity.py
  - custom_components/energy_manager/nordpool_adapter.py
  - custom_components/energy_manager/auto_detect.py
autonomous: true

must_haves:
  truths:
    - "HACS-compliant directory structure exists with valid manifest.json and hacs.json"
    - "All shared config keys and constants are defined in a single const.py module"
    - "Nordpool adapter can detect HACS vs native variant and fetch prices from either"
    - "Auto-detection module can scan entity registry for Nordpool, SigenStor, Easee, and car integrations"
  artifacts:
    - path: "custom_components/energy_manager/manifest.json"
      provides: "HACS installation metadata"
      contains: "energy_manager"
    - path: "custom_components/energy_manager/hacs.json"
      provides: "HACS repository metadata"
      contains: "Energy Manager"
    - path: "custom_components/energy_manager/const.py"
      provides: "Shared constants for all modules"
      contains: "DOMAIN"
    - path: "custom_components/energy_manager/nordpool_adapter.py"
      provides: "Nordpool variant detection and price fetching"
      exports: ["detect_nordpool_type", "find_all_nordpool_sensors", "async_get_prices"]
    - path: "custom_components/energy_manager/auto_detect.py"
      provides: "Integration auto-detection for config flow"
      exports: ["auto_detect_nordpool", "find_sigenstor_entities", "find_easee_entities", "find_car_integrations"]
    - path: "custom_components/energy_manager/entity.py"
      provides: "Base entity class for future modules"
      contains: "EnergyManagerEntity"
  key_links:
    - from: "custom_components/energy_manager/nordpool_adapter.py"
      to: "custom_components/energy_manager/const.py"
      via: "imports NORDPOOL_TYPE_HACS, NORDPOOL_TYPE_NATIVE"
      pattern: "from .const import"
    - from: "custom_components/energy_manager/auto_detect.py"
      to: "homeassistant.helpers.entity_registry"
      via: "entity registry scanning"
      pattern: "er\\.async_get"
---

<objective>
Create the project scaffold and foundational modules that all other plans depend on: HACS-compliant file structure, shared constants, Nordpool price adapter (ported from PowerSaver), auto-detection module for config flow, and base entity class.

Purpose: Establishes the file structure and shared modules that Plans 02 and 03 import from. Without these foundations, neither the config flow nor the integration core can be built.

Output: Six files forming the integration skeleton and shared utility modules.
</objective>

<execution_context>
@/Users/johan.yourstone/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johan.yourstone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-infrastructure-price-foundation/01-CONTEXT.md
@.planning/phases/01-core-infrastructure-price-foundation/01-RESEARCH.md

# Reference: PowerSaver proven patterns (port, don't reinvent)
@/Users/johan.yourstone/Git/power_saver/custom_components/power_saver/nordpool_adapter.py
@/Users/johan.yourstone/Git/power_saver/custom_components/power_saver/const.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HACS-compliant project scaffold and shared constants</name>
  <files>
    custom_components/energy_manager/manifest.json
    custom_components/energy_manager/hacs.json
    custom_components/energy_manager/const.py
    custom_components/energy_manager/entity.py
  </files>
  <action>
Create the directory structure `custom_components/energy_manager/` and populate these four files:

**manifest.json:**
- `domain`: `"energy_manager"`
- `name`: `"Energy Manager"` (per user decision)
- `codeowners`: `["@jyourstone"]`
- `config_flow`: `true`
- `dependencies`: `[]` (reads other integrations via state machine, no Python imports)
- `documentation`: `"https://github.com/jyourstone/energy_manager"`
- `integration_type`: `"hub"` (manages multiple device types)
- `iot_class`: `"local_polling"` (polls local HA entities)
- `issue_tracker`: `"https://github.com/jyourstone/energy_manager/issues"`
- `requirements`: `[]` (no external PyPI deps)
- `version`: `"0.1.0"`
- `min_version`: `"2024.12.0"` (required for ConfigSubentryFlow)

**hacs.json:**
- `name`: `"Energy Manager"`
- `render_readme`: `true`
- `homeassistant`: `"2024.12.0"`

**const.py** -- define ALL constants needed across the integration (Plans 02 and 03 import from here):
- `DOMAIN = "energy_manager"`
- Nordpool config keys: `CONF_NORDPOOL_SENSOR`, `CONF_NORDPOOL_TYPE`
- Nordpool type values: `NORDPOOL_TYPE_HACS = "hacs"`, `NORDPOOL_TYPE_NATIVE = "native"`
- Module toggle keys: `CONF_BATTERY_ENABLED`, `CONF_EV_ENABLED`
- Battery config keys: `CONF_SOC_ENTITY`, `CONF_BATTERY_POWER_ENTITY`
- EV config keys: `CONF_CHARGER_STATUS_ENTITY`, `CONF_CHARGER_POWER_ENTITY`
- Car subentry keys: `CONF_CAR_NAME`, `CONF_BATTERY_CAPACITY`, `CONF_BATTERY_LEVEL_ENTITY`, `CONF_HOME_PLUGGED_ENTITY`
- Subentry type: `SUBENTRY_TYPE_CAR = "car"`
- Module IDs: `MODULE_BATTERY = "battery"`, `MODULE_EV = "ev"`
- Update interval: `PRICE_UPDATE_INTERVAL_MINUTES = 5`
- Config entry version: `CONFIG_VERSION = 1`, `CONFIG_MINOR_VERSION = 1`

**entity.py** -- base entity class for all future module entities:
- `EnergyManagerEntity(CoordinatorEntity)` with:
  - `_attr_has_entity_name = True`
  - Property `device_info` returning `DeviceInfo` with hub identifiers `(DOMAIN, entry.entry_id)`, `name="Energy Manager"`, `manufacturer="Energy Manager"`, `model="Hub"`, `entry_type=DeviceEntryType.SERVICE`
  - This base class will be subclassed by module-specific entities in future phases
  - Accept the config entry in `__init__` and store `self._entry_id = entry.entry_id`
  </action>
  <verify>
Verify with:
1. `python -c "import json; d=json.load(open('custom_components/energy_manager/manifest.json')); assert d['domain'] == 'energy_manager'"` -- manifest valid JSON with correct domain
2. `python -c "import json; d=json.load(open('custom_components/energy_manager/hacs.json')); assert d['name'] == 'Energy Manager'"` -- hacs.json valid
3. `python -c "import ast; ast.parse(open('custom_components/energy_manager/const.py').read()); print('OK')"` -- const.py is valid Python
4. `python -c "import ast; ast.parse(open('custom_components/energy_manager/entity.py').read()); print('OK')"` -- entity.py is valid Python
5. Grep `const.py` for all required constants: `DOMAIN`, `CONF_NORDPOOL_SENSOR`, `CONF_BATTERY_ENABLED`, `CONF_EV_ENABLED`, `CONF_CAR_NAME`, `SUBENTRY_TYPE_CAR`
  </verify>
  <done>
All four files exist with valid content. manifest.json and hacs.json are valid JSON. const.py defines all shared constants needed by Plans 02 and 03. entity.py defines the base entity class with hub device info.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port Nordpool adapter and create auto-detection module</name>
  <files>
    custom_components/energy_manager/nordpool_adapter.py
    custom_components/energy_manager/auto_detect.py
  </files>
  <action>
**nordpool_adapter.py** -- Port directly from PowerSaver (`/Users/johan.yourstone/Git/power_saver/custom_components/power_saver/nordpool_adapter.py`), adapting imports to use `energy_manager` const.py:

Port these functions (keep the proven logic, just update imports):
- `detect_nordpool_type(hass, entity_id) -> str` -- returns "hacs", "native", or "unknown"
- `find_all_nordpool_sensors(hass) -> list[tuple[str, str, str]]` -- scans entity registry for both HACS and native variants
- `auto_detect_nordpool(hass) -> tuple[str, str] | tuple[None, None]` -- convenience wrapper returning first found sensor
- `async_get_prices(hass, entity_id, nordpool_type) -> tuple[list[dict], list[dict]]` -- fetches today/tomorrow prices
- `_get_hacs_prices(hass, entity_id)` -- reads `raw_today`/`raw_tomorrow` attributes
- `_async_get_native_prices(hass, entity_id)` -- calls `nordpool.get_prices_for_date` service
- `_async_fetch_native_date(hass, config_entry_id, target_date)` -- single date fetch
- `_convert_native_response(response)` -- converts native MWh to kWh format
- `_get_friendly_name(hass, entity_id)` -- helper for display labels

Key differences from PowerSaver: Import `from .const import NORDPOOL_TYPE_HACS, NORDPOOL_TYPE_NATIVE` (same constant names, different module). All logic stays identical -- this is proven production code.

**auto_detect.py** -- New module for config flow auto-detection:

Create functions that scan the HA entity registry and config entries to find compatible integrations:

- `auto_detect_nordpool(hass) -> tuple[str | None, str | None]` -- delegates to `nordpool_adapter.auto_detect_nordpool()` (re-export for convenience)
- `find_sigenstor_entities(hass) -> dict[str, str]` -- scan entity registry for SigenStor platform entities. Look for config entries with domain containing "sigen". For each, scan entities for patterns: "battery_state_of_charge" -> `soc_entity`, "battery_power" -> `battery_power_entity`. Return dict mapping config keys to entity IDs. Return empty dict if not found.
- `find_easee_entities(hass) -> dict[str, str]` -- scan for Easee integration. Look for config entries with domain "easee". For entities: "status" -> `charger_status_entity`, "power" -> `charger_power_entity`. Return dict mapping config keys to entity IDs. Return empty dict if not found.
- `find_car_integrations(hass) -> list[dict[str, str]]` -- scan for Skoda Connect and VW We Connect integrations. For each car found, return dict with `name` (friendly name from device or entity), `battery_level_entity` (SOC sensor), `platform` ("skoda" or "volkswagen"). Return empty list if none found. Search config entries for domains containing "skoda" or "volkswagen"/"vw".

All auto-detection functions must:
1. Use `er.async_get(hass)` for entity registry scanning
2. Use `hass.config_entries.async_entries()` for config entry scanning
3. Return empty results (not raise) when nothing found
4. Log found entities at DEBUG level
  </action>
  <verify>
Verify with:
1. `python -c "import ast; ast.parse(open('custom_components/energy_manager/nordpool_adapter.py').read()); print('OK')"` -- valid Python
2. `python -c "import ast; ast.parse(open('custom_components/energy_manager/auto_detect.py').read()); print('OK')"` -- valid Python
3. Grep `nordpool_adapter.py` for: `detect_nordpool_type`, `find_all_nordpool_sensors`, `async_get_prices`, `_convert_native_response`
4. Grep `auto_detect.py` for: `find_sigenstor_entities`, `find_easee_entities`, `find_car_integrations`
5. Grep both files for `from .const import` -- must import from local const module
  </verify>
  <done>
nordpool_adapter.py is a faithful port from PowerSaver with updated imports. auto_detect.py provides entity/config entry scanning for all supported integrations (Nordpool, SigenStor, Easee, Skoda/VW). Both modules return empty results gracefully when integrations are not found.
  </done>
</task>

</tasks>

<verification>
1. Directory `custom_components/energy_manager/` exists with all 6 files
2. `manifest.json` has `domain: "energy_manager"`, `integration_type: "hub"`, `min_version: "2024.12.0"`
3. `const.py` exports all constants needed by config_flow.py (Plan 02) and __init__.py (Plan 03)
4. `nordpool_adapter.py` handles both HACS (`raw_today` attribute) and native (service call) Nordpool variants
5. `auto_detect.py` scans for Nordpool, SigenStor, Easee, and car (Skoda/VW) integrations
6. All Python files parse without syntax errors
</verification>

<success_criteria>
All six foundational files exist and are syntactically valid. The Nordpool adapter preserves PowerSaver's proven price-fetching logic. The auto-detection module covers all four integration families. const.py defines every constant that Plans 02 and 03 will need (no missing imports in downstream plans).
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure-price-foundation/01-01-SUMMARY.md`
</output>
