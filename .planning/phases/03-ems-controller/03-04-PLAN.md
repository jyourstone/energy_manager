---
phase: 03-ems-controller
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/energy_manager/auto_detect.py
  - custom_components/energy_manager/config_flow.py
  - custom_components/energy_manager/coordinator.py
  - tests/test_auto_detect_ems.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Auto-detection finds SigenStor charge limit entity (sensor.sigen_battery_ess_rated_charging_power)"
    - "Auto-detection finds SigenStor discharge limit entity (sensor.sigen_plant_ess_rated_discharging_power)"
    - "Auto-detection finds L-current entity via phase_X_active_power fallback patterns"
    - "Auto-detection finds PV power entity via global fallback scan when not under sigen config entry"
    - "Config flow EMS step accepts sensor domain for charge/discharge limit fields"
    - "EMSCoordinator logs a warning at startup when L-current entity is not configured"
  artifacts:
    - path: "custom_components/energy_manager/auto_detect.py"
      provides: "Fixed find_sigenstor_ems_entities() with correct domains, patterns, and fallbacks"
      contains: "ess_rated_charging"
    - path: "custom_components/energy_manager/config_flow.py"
      provides: "Updated EMS step entity selectors accepting sensor+number domains"
      contains: "domain=[\"sensor\", \"number\"]"
    - path: "custom_components/energy_manager/coordinator.py"
      provides: "Startup warning for unconfigured L-current entity"
      contains: "L-current entity not configured"
    - path: "tests/test_auto_detect_ems.py"
      provides: "Regression tests for all 4 fixed auto-detection patterns"
  key_links:
    - from: "auto_detect.py find_sigenstor_ems_entities()"
      to: "config_flow.py async_step_ems()"
      via: "detected dict used as suggested_values"
      pattern: "find_sigenstor_ems_entities"
    - from: "config_flow.py _create_entry()"
      to: "coordinator.py EMSCoordinator.__init__()"
      via: "entry.options entity config keys"
      pattern: "CONF_L_CURRENT_ENTITY"
---

<objective>
Fix EMS entity auto-detection and fuse headroom input wiring

Purpose: UAT revealed 4 bugs in find_sigenstor_ems_entities() causing charge limit, discharge limit, L-current, and PV power entities to not be detected. This cascades to fuse_headroom_amps always reading 18A (the L-current entity is empty, so current load is assumed zero). Fixing auto-detection resolves both UAT gaps.

Output: Corrected auto_detect.py with proper domain filters, entity_id patterns, and global fallback scans. Updated config_flow.py entity selectors. Startup warning in EMSCoordinator. Regression tests.
</objective>

<execution_context>
@/Users/johan.yourstone/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johan.yourstone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ems-controller/03-UAT.md
@.planning/debug/ems-autodetect-missing-entities.md
@.planning/debug/fuse-headroom-always-18a.md
@.planning/phases/03-ems-controller/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix auto-detection patterns and config flow entity selectors</name>
  <files>
    custom_components/energy_manager/auto_detect.py
    custom_components/energy_manager/config_flow.py
  </files>
  <action>
Fix 4 bugs in find_sigenstor_ems_entities() in auto_detect.py:

**BUG 1 - Charge limit (lines 164-179):**
- Change domain guard from `entity_entry.domain == "number"` to accept BOTH "number" and "sensor" domains: `entity_entry.domain in ("number", "sensor")`
- Add patterns: "ess_rated_charging", "rated_charging_power" alongside existing "max_charging_limit", "ess_max_charging"
- Keep existing patterns for backward compatibility with other SigenStor firmware versions

**BUG 2 - Discharge limit (lines 181-196):**
- Same domain change: `entity_entry.domain in ("number", "sensor")`
- Add patterns: "ess_rated_discharging", "rated_discharging_power" alongside existing "max_discharging_limit", "ess_max_discharging"

**BUG 3 - L-current (lines 198-215 and fallback 236-252):**
- Add patterns to the SigenStor scan: "phase_a_active_power", "phase_active_power", "grid_phase" (to match sigen_plant_grid_phase_X_active_power)
- Update the global fallback scan (lines 236-252) to also check for "phase_a_active_power" and "grid_phase" patterns, not just "highest_l_current" and "l_current"
- NOTE: These are kW power sensors, not amp sensors. The entity will be detected and the user can manually confirm in the config flow. The coordinator already handles float values generically via _read_float_state. The unit mismatch (kW vs A) means the fuse headroom calculation will be approximate when using power-based entities -- this is acceptable as a "better than nothing" auto-suggestion that the user can replace with a proper current sensor.

**BUG 4 - PV power (after line 234):**
- Add a global fallback scan for PV power (mirroring L-current fallback structure at lines 236-252)
- After the sigen config entry loop, if CONF_PV_POWER_ENTITY is still not found, scan ALL sensor entities for "pv_power" with preference for entities containing "sigen" in entity_id
- If multiple matches found, prefer entity_id containing "plant" over "inverter" (plant-level = total after clipping, more accurate)

**Config flow update in config_flow.py async_step_ems():**
- Change CONF_CHARGE_LIMIT_ENTITY EntitySelector from `EntitySelectorConfig(domain="number")` to `EntitySelectorConfig(domain=["sensor", "number"])` -- the actual SigenStor entities are sensors, not number entities
- Change CONF_DISCHARGE_LIMIT_ENTITY EntitySelector from `EntitySelectorConfig(domain="number")` to `EntitySelectorConfig(domain=["sensor", "number"])`

Do NOT change any other config flow logic, only the two domain selectors.
  </action>
  <verify>
    Run `cd /Users/johan.yourstone/Git/energy_manager && python -m pytest tests/ -x -q` -- all existing 40 tests must pass (no regressions).
    Manually verify auto_detect.py contains "ess_rated_charging" pattern and domain check includes "sensor".
    Manually verify config_flow.py contains `domain=["sensor", "number"]` for charge and discharge limit selectors.
  </verify>
  <done>
    find_sigenstor_ems_entities() matches entity names like "sigen_battery_ess_rated_charging_power" (sensor domain), "sigen_plant_ess_rated_discharging_power" (sensor domain), "sigen_plant_grid_phase_a_active_power" (L-current fallback), and "sigen_plant_pv_power" (PV global fallback). Config flow EMS step accepts both sensor and number domains for charge/discharge limit fields. All 40 existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add startup warning and auto-detect regression tests</name>
  <files>
    custom_components/energy_manager/coordinator.py
    tests/test_auto_detect_ems.py
  </files>
  <action>
**Coordinator startup warning (coordinator.py):**
In EMSCoordinator._async_setup(), after the L-current entity listener registration block (around line 586-593), add a warning log when self._l_current_entity is empty:

```python
if not self._l_current_entity:
    _LOGGER.warning(
        "L-current entity not configured -- fuse headroom will assume 0A load. "
        "Configure an L-current sensor in the EMS settings for dynamic fuse protection."
    )
```

This gives the user a clear log message explaining why fuse headroom is static, without changing any behavior.

**Auto-detect regression tests (tests/test_auto_detect_ems.py):**
Create a new test file with lightweight unit tests for find_sigenstor_ems_entities(). Use the existing test infrastructure pattern from conftest.py (importlib MetaPathFinder for HA stubs).

Tests to write (each verifying a specific fix):

1. `test_detects_charge_limit_sensor_domain` -- Register a mock entity with domain="sensor" and entity_id containing "ess_rated_charging_power" under a sigen config entry. Assert CONF_CHARGE_LIMIT_ENTITY is returned.

2. `test_detects_discharge_limit_sensor_domain` -- Same pattern for "ess_rated_discharging_power". Assert CONF_DISCHARGE_LIMIT_ENTITY is returned.

3. `test_detects_l_current_via_phase_active_power` -- Register entity with "phase_a_active_power" pattern. Assert CONF_L_CURRENT_ENTITY is returned (either from sigen scan or global fallback).

4. `test_detects_pv_power_via_global_fallback` -- Register a PV power entity NOT under a sigen config entry (simulating the real-world scenario). Assert CONF_PV_POWER_ENTITY is returned via the global fallback.

5. `test_pv_fallback_prefers_plant_over_inverter` -- Register both "sigen_plant_pv_power" and "sigen_inverter_pv_power" entities globally. Assert the plant variant is preferred.

6. `test_ems_select_still_detected` -- Sanity check that the existing EMS select detection (which was working) still works after changes.

Follow the existing test pattern: use a mock hass, mock entity registry, and mock config entries. The conftest.py HA stubs should provide enough of the entity_registry API. If not, create minimal mocks inline.

Each test should be independent and isolated. Use `pytest` parametrize where patterns overlap.
  </action>
  <verify>
    Run `cd /Users/johan.yourstone/Git/energy_manager && python -m pytest tests/test_auto_detect_ems.py -v` -- all new tests pass.
    Run `cd /Users/johan.yourstone/Git/energy_manager && python -m pytest tests/ -x -q` -- all tests pass (40 existing + 6 new = 46).
    Grep coordinator.py for "L-current entity not configured" -- warning exists.
  </verify>
  <done>
    6 new regression tests in test_auto_detect_ems.py all pass, covering charge limit sensor domain, discharge limit sensor domain, L-current phase_active_power fallback, PV global fallback, PV plant-over-inverter preference, and EMS select sanity. EMSCoordinator logs a warning when L-current entity is unconfigured. Total test count is 46 with zero failures.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -m pytest tests/ -x -q` -- 46 tests pass, zero failures
2. `grep -n "ess_rated_charging" custom_components/energy_manager/auto_detect.py` -- pattern exists
3. `grep -n "domain=\[" custom_components/energy_manager/config_flow.py` -- multi-domain selectors exist
4. `grep -n "L-current entity not configured" custom_components/energy_manager/coordinator.py` -- warning exists
5. `grep -c "def test_" tests/test_auto_detect_ems.py` -- shows 6 test functions

Both UAT gaps are addressed:
- Gap 1 (auto-detect misses): All 4 pattern bugs fixed with regression tests
- Gap 2 (fuse headroom static 18A): Root cause was Gap 1 -- once auto-detect finds L-current entity, headroom will be dynamic. Startup warning alerts user if still unconfigured.
</verification>

<success_criteria>
- All 46 tests pass (40 existing + 6 new auto-detect regression tests)
- find_sigenstor_ems_entities() detects entities matching real-world SigenStor naming
- Config flow EMS step accepts sensor domain for charge/discharge limit entities
- EMSCoordinator warns on startup when L-current is unconfigured
- No changes to ems_controller.py (pure module is correct, bug was in inputs)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ems-controller/03-04-SUMMARY.md`
</output>
